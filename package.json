{
  "name": "rss-feed-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram bot for RSS, X (Twitter), and YouTube monitoring",
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply SQL --local",
    "db:migrate:remote": "wrangler d1 migrations apply SQL --remote"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "fast-xml-parser": "5.10.1"
  },
  "devDependencies": {
    "wrangler": "4.111.0"
  }
}
